FROM ubuntu:latest

RUN apt-get update && apt-get install -y curl python3-pip
RUN curl -fsSL http://example.com/install.sh | sh

COPY . /app
WORKDIR /app

RUN pip3 install -r security-fixtures/requirements-vulnerable.txt

EXPOSE 22
USER root
CMD ["python3", "-m", "http.server", "80"]
